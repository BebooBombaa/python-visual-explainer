# 🤝 دليل المساهمة | Contributing Guide

أهلاً بك! سعداء جداً برغبتك في المساهمة في **مُفسّر بايثون المرئي**.
هذا المشروع تعليمي في المقام الأول، لذا **كل المساهمات مرحّب بها** — حتى تصحيح خطأ إملائي في الشرح العربي يفيد آلاف الطلاب. 🙏

---

## 🌱 مساهمات مناسبة للمبتدئين

إذا كانت هذه أول مساهمة لك في مشروع مفتوح المصدر، ابدأ من هنا:

| المستوى | المهمة | الملف |
| :--- | :--- | :--- |
| ⭐ سهل جداً | إضافة مثال تعليمي جديد | `src/data/examples.ts` |
| ⭐ سهل جداً | تحسين صياغة شرح عربي | `src/pythonParser.ts` |
| ⭐⭐ متوسط | إضافة تصنيف أمثلة جديد | `src/data/examples.ts` |
| ⭐⭐ متوسط | تحسين الألوان أو الأيقونات | `src/components/*.tsx` |
| ⭐⭐⭐ متقدم | دعم أمر بايثون جديد في المُحلّل | `src/pythonParser.ts` |
| ⭐⭐⭐ متقدم | ميزة جديدة من خارطة الطريق | متعدد |

---

## 🚀 خطوات المساهمة

### 1. جهّز بيئتك

```bash
# انسخ المستودع (Fork) من واجهة GitHub، ثم:
git clone https://github.com/YOUR-BebooBombaa/python-visual-explainer.git
cd python-visual-explainer
npm install
npm run dev
```

### 2. أنشئ فرعاً جديداً

```bash
git checkout -b feature/اسم-الميزة
# أو
git checkout -b fix/وصف-الإصلاح
```

### 3. نفّذ تعديلاتك

تأكد قبل الإرسال من أن كل شيء يعمل:

```bash
npx tsc --noEmit   # لا أخطاء أنواع
npm run build      # البناء ينجح
```

### 4. أرسل التعديلات

```bash
git add .
git commit -m "feat: إضافة مثال FizzBuzz"
git push origin feature/اسم-الميزة
```

ثم افتح **Pull Request** من واجهة GitHub.

---

## 📝 صيغة رسائل الـ Commit

نتبع صيغة [Conventional Commits](https://www.conventionalcommits.org/):

| البادئة | الاستخدام | مثال |
| :--- | :--- | :--- |
| `feat:` | ميزة جديدة | `feat: دعم حلقة while المتداخلة` |
| `fix:` | إصلاح خطأ | `fix: تصحيح حساب عدد تكرارات range` |
| `docs:` | توثيق فقط | `docs: تحديث دليل التثبيت` |
| `style:` | تنسيق/ألوان | `style: تحسين تباين البطاقات` |
| `refactor:` | إعادة هيكلة | `refactor: فصل دوال التقييم` |
| `content:` | أمثلة تعليمية | `content: إضافة 3 أمثلة عن القوائم` |
| `chore:` | مهام صيانة | `chore: تحديث الحزم` |

---

## 🧩 إضافة مثال تعليمي (الأكثر طلباً)

افتح `src/data/examples.ts` وأضف كائناً جديداً للمصفوفة:

```ts
{
  id: 'unique-id',              // معرّف فريد بالإنجليزية
  titleAr: 'العنوان بالعربية',
  titleEn: 'English Title',
  descriptionAr: 'شرح مختصر لما يعلّمه المثال',
  category: 'أساسيات',           // أساسيات | الشروط | الحلقات | الدوال | البنى البيانات | مشاريع
  code: `# الكود هنا
x = 10
print(x)`
},
```

### ✅ معايير قبول المثال

- [ ] يستخدم أوامر يدعمها المُحلّل حالياً (راجع جدول الدعم في README)
- [ ] قصير ومركّز — يُفضّل أقل من 20 سطراً
- [ ] يعلّم **مفهوماً واحداً** بوضوح
- [ ] يحتوي على تعليقات عربية توضيحية `#`
- [ ] مُجرّب فعلياً في الموقع وظهر شرحه بشكل صحيح

---

## 🧠 إضافة دعم لأمر بايثون جديد

راجع **[دليل المعمارية](docs/ARCHITECTURE.md)** — يشرح بالتفصيل كيف يعمل المُحلّل وكيف تضيف نوع خطوة (`stepType`) جديداً في 4 خطوات.

باختصار:

1. أضف النوع الجديد إلى `stepType` في واجهة `CodeStep`.
2. أضف كتلة `if (match) { ... }` في دالة `analyzePythonCode` **قبل** الحالة العامة.
3. أضف الأيقونة واللون في `stepIcons` داخل `StepVisualizer.tsx`.
4. أضف نفس الشيء في `flowIcons` داخل `FlowOverview.tsx`.

---

## 🐛 الإبلاغ عن خطأ

عند فتح Issue، أرفق دائماً:

1. **كود بايثون** الذي سبّب المشكلة (انسخه كاملاً)
2. **ما توقّعت أن يظهر**
3. **ما ظهر فعلياً** (صورة إن أمكن)
4. المتصفح ونظام التشغيل

---

## 🎨 إرشادات الكود

- **TypeScript** لكل الملفات — تجنّب `any` قدر الإمكان
- **Tailwind CSS** للتنسيق — لا تكتب CSS منفصلاً إلا للضرورة
- **RTL أولاً** — تذكّر أن الواجهة عربية، استخدم `mr/ml` بحذر
- **مكوّنات صغيرة** — إذا تجاوز المكوّن 200 سطر، فكّر في تقسيمه
- **أسماء واضحة** — بالإنجليزية للكود، بالعربية للنصوص الظاهرة للمستخدم
- **لا تعديل** على `package.json` أو `vite.config.ts` دون نقاش في Issue أولاً

---

## 💬 أسئلة؟

افتح [Discussion](../../discussions) أو [Issue](../../issues/new/choose) — سنرد بأسرع وقت.

**شكراً لمساهمتك في تعليم الطلاب العرب البرمجة! 🎓❤️**
